// created sumletters(a, b) to return true if the lengths of both inputs match.
//
// returns false when eaither argument is null or undefined.


const sumLetters = (a, b) => a?.length === b?.length



