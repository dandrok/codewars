import { test, it, expect, describe } from "vitest";

// test("new", () => {
//   //   it("should add two numbers", () => {
//   expect(1 + 1).toBe(2);
//   //   });
// });

describe("testing tests", () => {
  it("should add two numbers", () => {
    expect(1 + 1).toBe(2);
  });
});
